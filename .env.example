# =============================================================================
# ENVIRONMENT VARIABLES - Copy this file to .env and fill in the values
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port the Express server listens on (default: 8000)
PORT=8000

# Node environment: "development" or "production"
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/task_tracker

# -----------------------------------------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------------------------------------

# The base URL where your backend server is running
# Development: http://localhost:8000
# Production: https://your-domain.com (or your server's URL)
BETTER_AUTH_URL=http://localhost:8000

# Secret key for signing auth tokens (generate a random 32+ character string)
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Comma-separated list of allowed origins for CORS
# Development: http://localhost:5173 (Vite dev server)
# Production: https://your-domain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000

# -----------------------------------------------------------------------------
# Frontend (Vite) - Variables prefixed with VITE_ are exposed to the client
# -----------------------------------------------------------------------------

# API URL for the frontend (only needed if frontend/backend are on different origins)
# When running on the same origin, leave empty or don't set
# VITE_API_URL=

# -----------------------------------------------------------------------------
# Production Deployment Notes
# -----------------------------------------------------------------------------
#
# For production deployment on a single platform (frontend + backend together):
#
# 1. Set NODE_ENV=production
#
# 2. Set BETTER_AUTH_URL to your production domain:
#    BETTER_AUTH_URL=https://your-app.railway.app (or your platform's URL)
#
# 3. Set ALLOWED_ORIGINS to your production domain:
#    ALLOWED_ORIGINS=https://your-app.railway.app
#
# 4. Generate a secure BETTER_AUTH_SECRET:
#    openssl rand -base64 32
#
# 5. Set DATABASE_URL to your production database:
#    DATABASE_URL=postgresql://user:pass@host:5432/dbname
#
# 6. Build and run:
#    pnpm build
#    node server/index.js (or tsx server/index.ts)
#
# =============================================================================
