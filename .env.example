# ===========================================
# Frontend Configuration (Vite)
# ===========================================

# Backend API URL for the frontend to communicate with
# This is used by the auth client and tRPC client
# In production, set this to your backend server URL
VITE_API_URL=http://localhost:8000

# ===========================================
# Better Auth Configuration
# ===========================================

# Secret key used for signing tokens and encrypting session data
# Generate a secure random string (at least 32 characters recommended)
# You can generate one using: openssl rand -base64 32
BETTER_AUTH_SECRET='your-secure-random-secret-key-here'

# Base URL where your backend server is running
# Used by Better Auth for callbacks and redirects
BETTER_AUTH_URL=http://localhost:8000

# ===========================================
# CORS Configuration
# ===========================================

# Comma-separated list of allowed origins for CORS
# In development, this is your Vite dev server URL
# In production, this should be your frontend domain(s)
ALLOWED_ORIGINS=http://localhost:5173

# ===========================================
# Database Configuration
# ===========================================

# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database_name]
# Example: postgresql://postgres:mypassword@localhost:5432/task_tracker
DATABASE_URL='postgresql://username:password@localhost:5432/task_tracker'
